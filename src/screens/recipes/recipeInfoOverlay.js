import { SafeAreaView, View, Text } from "react-native";

export default function RecipeInfoOverlay() {
	return (
		<SafeAreaView>
			<View>
				<Text>Recipe info overlay</Text>
			</View>
		</SafeAreaView>
	);
}
